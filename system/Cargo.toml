[package]
name = "system"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "system"
test = false
doctest = false
bench = false

[features]
nightly = []

[dependencies]
tau = { path = "../tau" }
bitflags = { version = "2.10" }
noop-waker = { version = "0.1" }
