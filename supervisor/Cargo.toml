[package]
name = "supervisor"
version = "0.1.0"
edition = "2024"

[dependencies]
panic-never = { version = "0.1.0", optional = true }
memoffset = { version = "0.9.1" }
bitfield-struct = { version = "0.10.1" }
elf = { version = "0.7.4", default-features = false }
thiserror-no-std = { version = "2.0" }
tau = { path = "../tau" }
