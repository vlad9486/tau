[package]
name = "supervisor"
version = "0.1.0"
edition = "2024"

[lib]
name = "supervisor"
test = false
doctest = false
bench = false

[[bin]]
name = "loader"
test = false
doctest = false
bench = false

[[bin]]
name = "supervisor"
test = false
doctest = false
bench = false

[features]
nightly = []

[dependencies]
panic-never = { version = "0.1.0", optional = true }
memoffset = { version = "0.9.1" }
bitfield-struct = { version = "0.12.1" }
elf = { version = "0.8.0", default-features = false }
tau = { path = "../tau" }
